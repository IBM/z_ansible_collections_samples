---
# vault.yml file for the sample playbooks of the IBM Z HMC Collection

hmc_auth:

  myhmc13:  # nickname of the HMC, must match the nickname in the inventory file
    userid: myuserid
    password: mypassword
    verify: false   # optional, only if you need to disable certificate validation

  # ... more HMC entries, as needed
